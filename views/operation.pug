extends layout

block content
    h1= title

    if globalData.bulbCount
        div.resetForm
            form(name="bulbCountForm", method="post")
                div.actions
                    input(type="submit", name="submit", value="reset")
        div.setupInfo
            span.label Here is the bulb count:
            span.bulbCount #{globalData.bulbCount}
